# TCC CTCS-2 改造防中断上下文

> 此文件随代码提交并在每个阶段更新。重新连接后，先读取本文件、总控方案、当前阶段子方案和 `git log --oneline --decorate -10`，从“下一步”继续，不重复已完成阶段。

## 仓库状态

- 仓库：`git@github.com:zixin-zhu/TCC-project.git`
- 当前开发分支：`codex/dual-station-dashboard`
- 已完成基线分支：`codex/ctcs2-rebuild`（HEAD `5a8e06e`）
- 基线提交：`d62e396`（Initial version）
- 工作树：`/Users/zhu/Desktop/TCC-project/.worktrees/ctcs2-rebuild`
- IDE 约束：如需 IDE，只使用 PyCharm。
- UI 约束：双站同屏采用用户参考图“方案一：经典控制台风格”；浅灰白背景、
  深蓝标题栏、蓝色选中导航、白色细边框面板和紧凑表格。按钮和设备必须来自
  当前 CTCS-2 作业要求与软件真实功能，不得照抄示意图中的虚构对象或空功能。

## 固定工程约束

1. 一次只完成一个阶段，验收通过后独立提交并推送。
2. 新功能严格执行测试先行；生产代码使用类型标注、模块化边界和必要的详细中文注释。
3. 旧代码在阶段 7 确认无引用前不删除；用户已有修改不得覆盖。
4. JSON/HEX/CRC32 是 `simulation_envelope`，不宣称真实应答器位流或安全通信。
5. CTCS-3 论文只提供架构和教学展示参考，核心业务保持 CTCS-2。

## 环境和已知问题

- 系统 Python：3.9.6；目标方案要求后续提供 Python 3.11/3.12 可重建环境。
- 初始仓库没有测试文件和依赖清单。
- macOS 系统 Python 默认字节码缓存目录受沙箱限制；基线验证使用 `PYTHONPYCACHEPREFIX=/private/tmp/tcc-pycache`。
- 原代码存在两套入口、信号双轨状态、提前改方、网络线程边界等已知问题，详见总控方案。
- 2026-09-25 父目录由“列控课设”改名为 `TCC-project`，linked worktree 两侧
  绝对路径已在双站同屏阶段 0 精确修复；原指针备份位于
  `/private/tmp/tcc-worktree-pointer-backup/`。
- 执行计划技能的隐藏账本目录受当前沙箱限制，因此执行裁定、阶段证据和下一步
  统一记录在本文件；这是 Git 管理的长期防中断事实来源。

## 阶段状态

| 阶段 | 状态 | 提交 | 推送 | 验收摘要 |
|---|---|---|---|---|
| 1 工程/配置/领域模型 | 已完成 | `9276611` | 已推送 | 20 项测试；A/B 配置校验通过 |
| 2 编码与点灯 | 已完成 | `832339b` | 已推送 | 38 项全量测试；正反向/故障/边界/点灯通过 |
| 3 应答器/LEU/限速 | 已完成 | `197bfcf` | 已推送 | 16 项阶段测试；全量 54 项通过 |
| 4 双站网络 | 已完成 | `2474819` | 已推送 | 49 项阶段网络/配置测试；全量 103 项通过 |
| 5 区间改方 | 已完成 | `848d42d` | 已推送 | 单一权威方案；50 项阶段测试、144 项全量测试通过；最终复审无严重问题 |
| 6 UI/持久化 | 已完成 | `9f14216` | 已推送 | 全量 183 项通过；最终复审无 Critical/Important |
| 7 集成交付 | 已完成 | `1169ac4` | 已推送 | 196 项通过；双站实测通过；最终复审无 Critical/Important |

## 双站同屏改造阶段状态

设计基线与阶段方案入口：`docs/dual-station-plans/README.md`。

| 阶段 | 状态 | 提交 | 推送 | 验收摘要 |
|---|---|---|---|---|
| 0 工作区/Git 恢复 | 已完成 | `80848c8` | 已推送 | Git 恢复；196 项通过；复审无 Critical/Important |
| 1 单进程双运行时 | 已完成 | `172a00f` | 已推送 | 206 项通过；真实 TCP 双站健康；复审无 Critical/Important |
| 2 单站 UI 组件化 | 已完成 | `fba656d` | 已推送 | 212 项通过；A/B 截图目视通过；复审无 Critical/Important |
| 3 双站总览/线路图 | 已完成 | `d219a2b` | 已推送 | 229 项通过；真实双站窗口健康；复审无 Critical/Important |
| 4 双站操作/列车 | 已完成待提交 | — | — | 259 项通过；真实演练通过；最终复审无 Critical/Important |
| 5 集成验收/交付 | 未开始 | — | — | — |

### 阶段 0 执行账本

- 已确认旧 worktree 分支 `codex/ctcs2-rebuild`、HEAD `5a8e06e` 和索引完整。
- 已备份并修复 worktree `.git` 与主仓库 `worktrees/.../gitdir` 两侧绝对路径。
- 已创建专用分支 `codex/dual-station-dashboard`，未改写原分支与 `main`。
- Ruling：隐藏 `.superpowers` 账本因沙箱拒绝写入，改用本文件记录执行事实；
  若此裁定错误，影响仅是不能使用技能自带 task-start/task-done 辅助脚本，
  不影响代码、Git 历史、测试和用户要求的断线恢复。
- A/B 配置校验、`git diff --check` 和全量回归均通过，结果为 `196 passed`。
- 独立复审提出的旧执行索引和提交清单问题已修复，最终无 Critical/Important。
- 方案与恢复提交 `80848c8` 已推送到 `origin/codex/dual-station-dashboard`。
- 下一步：执行 `01-dual-runtime-lifecycle.md`，测试先行实现单进程双运行时。

### 阶段 1 执行账本

- 新增 `DualStationApplication`，A 完成 `bind + listen` 并发出 Qt 就绪信号后
  才启动 B；重复就绪信号不会重复启动 B。
- A/B 配置在创建数据库前完成站点 ID、SERVER/CLIENT 角色、对站 ID 和共享
  host/port 交叉校验；B 装配失败时主动关闭已创建的 A 控制器与仓储。
- 新增推荐入口 `run_dual.py`；`--validate-only` 不创建 QApplication、数据库
  或线程，正式模式在阶段 3 主窗口完成前只显示无虚假按钮的状态占位窗。
- 启动线程的 bind/listen 等异常统一转成 `error_occurred`，A 启动失败会阻止 B。
- 关闭首次严格执行 B→A；已成功站点不重复关闭，超时站点允许再次重试，
  双方成功后保持幂等，避免窗口永久卡在关闭失败并遗留资源。
- 新增真实集成验收：两套独立 SQLite、真实回环 TCP 握手、双方达到
  `HEALTHY`、反向关闭后两条 Qt 网络线程均退出。
- 阶段定向测试 `17 passed`；修复复审问题后的全量回归 `206 passed`；
  compileall、`git diff --check`、A/B/dual 配置校验均通过。
- 独立复审发现 1 个关闭失败缓存 Important，已用两项失败先行测试修复；
  二次复审无 Critical/Important。
- 功能提交 `172a00f` 已推送到 `origin/codex/dual-station-dashboard`。
- 下一步：执行 `02-station-ui-refactor.md`，先锁定现有九页 UI 和生命周期行为，
  再提取 `StationDetailWidget` 与集中经典控制台主题。

### 阶段 2 执行账本

- 将原 `TccMainWindow` 的九页业务界面完整提取为可嵌入
  `StationDetailWidget`；组件只显示快照和调用本站控制器，不停止网络、不关闭
  控制器或数据库。
- `include_train_page=False` 可为后续双站窗口让出列车页，由阶段 4 的唯一联合
  列车协调器接管；单站模式默认仍保留原九页和全部真实按钮。
- `TccMainWindow` 改为兼容壳；正式入口明确传入 `ApplicationRuntime` 作为
  lifecycle，运行时关闭控制器后窗口不再二次关闭。无 lifecycle 的测试/截图
  模式仍由窗口关闭一次控制器；关闭失败会拒绝退出并恢复先前活动的列车定时器。
- 新增集中 `CLASSIC_CONSOLE_QSS` 和动态语义属性工具，落实方案一的深蓝完整
  标题栏、浅灰白工作区、蓝色选中页签、白色细边框面板、紧凑表格与按钮；
  未使用固定坐标或平台专属字体。
- 1280×800 下逐页验证九页可访问，关键动作按钮齐全且无空按钮；表格采用
  按内容与末列拉伸的混合策略并保留滚动能力。
- 生成并目视检查 A/B 单站离屏截图；截图只证明样式，业务行为由测试证明。
- 阶段 UI 定向测试 `12 passed`，全量回归 `212 passed`；compileall、A/B 配置
  校验和 `git diff --check` 均通过；独立复审无 Critical/Important。
- 功能提交 `fba656d` 已推送到 `origin/codex/dual-station-dashboard`。
- 下一步：执行 `03-dashboard-corridor.md`，测试先行实现只读双站快照聚合、
  A/B 摘要卡、统一线路图和正式双站主窗口框架。

### 阶段 3 执行账本

- 新增不可变 `DualStationSnapshot` 与只读聚合器：A 方向为唯一权威；两站
  `HEALTHY`、方向一致、均未锁闭且共享区段一致时才允许联合行车。
- 聚合器在 A/B 首份快照到齐前不发布残缺模型，按站拒绝严格版本回退；同一
  业务版本内的通信、告警和计数变化仍允许刷新，避免旧健康快照覆盖新版降级。
- 新增 A/B 摘要卡和全局状态条，持续显示身份、角色、通信、版本、方向、锁闭、
  进路、边界码序、主信号继电器、LEU/报文、限速、告警及收发计数。
- 新增配置驱动联合线路图，区段顺序和宽度来自 topology 与实际长度；绘制状态、
  码序、名称、长度、全部信号、应答器、方向箭头和限速。共享区段不一致使用
  黄黑并标字；信号比较 A/B 灯色及 HJ/UJ/LJ，分歧不选边，聚合锁闭时不显示
  联合许可色。
- 新增正式 `DualStationMainWindow`，12 项左侧导航均对应实时页面；A/B 控制页
  嵌入阶段 2 组件，`run_dual.py` 已移除占位窗并使用正式双站窗口。
- 聚合安全锁闭会禁用两站建立进路、A 站改方及单站列车发车/启动控件；条件
  恢复后按本地门禁重新启用。人工/故障输入和取消等安全操作仍可用。
- 真实窗口测试发现 Qt 迟到 queued signal 可能在 controller 关闭后写入；根因
  修复为运行时关闭前关异步门，停止失败则恢复异步门与方向安全定时器。
- 新增可重复三分辨率截图脚本；1280×800、1440×900、1920×1080 均已生成并
  目视检查，关键数据可见且宽屏剩余空间用于最近事件表。
- 阶段定向测试 `30 passed`，全量回归 `229 passed`；真实 TCP 正式窗口达到
  `HEALTHY` 后干净关闭；compileall、配置校验和 `git diff --check` 均通过。
- 首轮复审发现版本回退与聚合锁闭未约束操作两个 Important，均以失败先行测试
  修复；二次复审无 Critical/Important。
- 功能提交 `d219a2b` 已推送到 `origin/codex/dual-station-dashboard`。
- 下一步：执行 `04-dual-operations-train.md`，先实现共享轨道输入适配器，再
  建立唯一联合列车协调器、真实双站操作页并迁移七个答辩场景。

### 阶段 4 执行账本（复审中）

- 新增 `SharedTrackInputAdapter`：A_/B_ 站内区段只写所属控制器，共享 Q 区段
  固定按 A→B 双写且只接受 TRAIN 来源；A 成功、B 失败时不回滚已生效占用，
  两站产生 `SHARED_INPUT_PARTIAL_FAILURE` 严重告警。
- 新增唯一 `DualTrainCoordinator` 和一个父属 500 ms `QTimer`；线路顺序完全
  来自 topology，支持 A→B 和严格逆序 B→A。发车需通信健康、方向一致、无锁闭、
  共享状态一致和发车进路成立；迁移严格先占下一段、再清上一段，任一步失败均
  停车并保留保守占用。
- 联合列车状态持续提供列车 ID、方向、区段、位置、当前/目标速度、前方距离、
  最后应答器和安全状态；复位只清 TRAIN 来源，不清人工、故障或分路不良。
- 新增真实双站操作页：轨道目标 A/B/共享、分站信号灯丝、A/B LEU 对照及
  逻辑/HEX/位流子页、分站临时限速、仅 A 发起改方、网络收发状态和联合列车。
  每次操作显示目标、成功/拒绝、原因和状态版本；无虚假的手工连接按钮。
- 七个既有场景 ID 保持不变，已增加目标导航页、目标站、前置条件、真实按钮
  objectName、详细步骤、预期结果和复位步骤，并全部改为 `run_dual.py` 单窗口操作。
- 首轮复审发现共享 CLEAR 部分失败可能清空 A 侧占用，以及第七场景缺少
  单窗口可恢复网络故障入口两个 Important。CLEAR 部分失败现会立即把 A 侧
  重新置为占用；复位任一项失败时保留列车位置并恢复当前区段保守占用。
- 网络 runner 新增线程安全教学故障开关；通信页提供“模拟B站网络中断/恢复”
  真实按钮，区间改方页提供“发起改方并立即中断B站”确定性场景按钮。故障时
  只关闭当前 TCP 会话，不停止线程或控制器；恢复后自动握手和全量同步。
- 真实重连演练暴露 Qt 队列竞态：合法全量同步可能先于 HEALTHY 信号到达，
  使方向恢复错误停在 FAULT_LOCKED。现以“协议已接受全量同步”为健康证据，
  在方向守卫前恢复连接门禁；真实 TCP 故障恢复连续 5 次通过。
- 关闭请求会不可逆地设置网络线程停止标志，因此即使等待超时、窗口拒绝退出，
  联合列车 timer 也始终保持安全停止，绝不基于旧 HEALTHY 快照恢复推进。
  联合列车结果栏现显示 A/B 实际最大状态版本。
- compileall、`git diff --check` 与全量回归 `259 passed`。真实回环 TCP 演练
  完成 A→B 到达、正常改方、B→A 到达、改方中断、自动重连、方向一致解锁以及
  两条网络线程干净关闭。
- 三轮独立复审中的 Important 均已修复，最终复审无 Critical/Important。
- 下一步：提交并推送阶段 4 功能提交，再记录独立检查点并进入阶段 5。

## 已完成：阶段 1

完成内容：

- 建立 `app/core` 强类型枚举、配置 dataclass、领域事件和多来源轨道状态。
- 实现站点、拓扑、进路、边界、应答器组、LEU 与默认报文的配置校验。
- 提供 A Server/B Client 正式配置、旧 G01~G08 迁移映射和统一 `run.py`。
- 建立 Python 3.12 `.venv`、pytest、PyQt5 和 Qt 环境诊断。
- 阶段验收：`20 passed`；A/B `--validate-only` 均通过；新代码编译通过。

阶段决定：有效轨道状态不变时不增加 `state_version`，即使较低优先级来源发生变化；这样避免被故障遮蔽的列车输入制造无意义同步版本。

新增环境发现：Qt 5 在包含中文的安装路径中可能把插件路径编码成 `????`。诊断脚本可定位真实插件；正式 GUI 启动的动态插件路径准备留在阶段 6 实现并测试，禁止硬编码用户绝对路径。

## 已完成：阶段 2

完成内容：

- 实现发车/接车轻量进路的方向、占用和冲突检查。
- 实现配置化 A→B/B→A 区间码序、站内检测码和发车进路跟随码。
- 邻站边界缺失/过期统一保护；只使用当前运行前方边界，忽略后方无关状态。
- 实现码序到灯色和 HJ/UJ/LJ 的唯一映射，以及红灯灯丝故障严重告警结果。
- 提供 `docs/examples/stage2-code-signal-example.md` 作为答辩解释样例。
- 阶段验收：阶段 2 测试 18 项，全量测试 `38 passed`。

阶段决定：`coding_rules.json` 中的 L/L2/L3/L5 序列是课程教学配置，不写死在算法中，也不宣称对应具体线路工程数据。

## 已完成：阶段 3

完成内容：

- 建立 ETCS-5、ETCS-21、ETCS-27、CTCS-1、CTCS-2 五类最小信息包定义和逻辑报文模板。
- 实现字段范围、未知字段、方向、模板包顺序及 CTCS-2 限速区间校验。
- 实现应答器组位置校验、LEU 正常选择、无匹配默认、断联/过期/报文错误故障默认。
- 实现临时限速预存、验证、执行、撤销、过期和时空重叠拒绝。
- 实现确定性的 `simulation_envelope` 规范 JSON、HEX 和 CRC32。
- 保存正常、限速、默认路径黄金场景；阶段测试 16 项，全量 `54 passed`。

阶段决定：不生成或展示为“真实 1023 位报文”；所有二进制/HEX 展示均明确标记为课程仿真封装。

## 已完成：阶段 4

完成内容：

- 实现 4 字节大端长度帧、65536 字节上限、规范 JSON 与 CRC32 仿真封装。
- 校验固定协议方向、版本、消息 ID、sequence、时间戳、状态版本和消息类型。
- 实现 HELLO/ACK 握手、握手前业务门禁、心跳、3500/6000 ms 降级/断开。
- 实现 A Server/B Client、B 站 1/2/5 秒退避重连和重连后的双向全量同步。
- socket 只在 runner/Qt worker 线程创建、读写与关闭，停止事件可中断退出。
- 接入 `PeerSyncService` 语义门禁；坏同步、版本回退和未知边界不进入控制器。
- 发送队列限制 256 条；新会话清空旧增量，避免过期状态跨重连补发。
- 提供协议技术说明与 Hypothesis 属性测试；全量回归 `103 passed`。

阶段决定：CRC32 明确只用于课程仿真的意外损坏检测，不宣称认证、加密或铁路安全通信能力；活性使用本站单调接收时钟，不依赖对站系统时钟。

## 已完成：阶段 5

完成内容：

- 替换旧版“批准即切向”逻辑，建立不依赖 Qt/socket 的四阶段改方状态机。
- 申请与批准前检查通信、快照、全区间、故障/分路、进路、事务和双方版本。
- APPROVE 只建立预留；A 站复核后提交唯一权威方向，B 站只应用同步投影。
- 完成 UUID、完整报文指纹幂等、LRU 上限、乱序/错事务/旧版本和拒绝终止规则。
- 实现改方领域消息到阶段 4 JSON/CRC 协议的严格角色适配与五类往返测试。
- 为网络 runner/Qt worker 增加仅用于日志/编排的 `message_sent` 信号。
- 增加统一运行态 Action 应用服务；锁闭时拒绝进路并强制所有信号保持红灯。
- 提交后的断线/超时不回滚权威方向，保持 `FAULT_LOCKED`，重连全量同步并复核守卫后解锁。
- 新增唯一生产协调器，强制先应用统一运行态的方向/锁闭，再交付网络消息。
- 阶段 5 相关测试 50 项、全量回归 144 项通过；最终独立复审未发现 Critical/Important。
- 功能提交 `848d42d` 已推送到 `origin/codex/ctcs2-rebuild`。

阶段决定：用户已确认 A Server 为唯一区间方向权威。网络分区时允许 B 的显示
投影暂时滞后，但它不是第二份可提交真值；只要通信或投影未确认，双方相关
行车作业都保持安全锁闭。这是课程仿真的安全降级模型，不宣称替代真实铁路
安全通信、冗余硬件或安全认证协议。

## 已完成：阶段 6

已完成内容：

- 建立 `TccController` 单一写入口，固定“状态、编码、信号、LEU、告警、同步、UI、日志”事件顺序。
- 建立告警去重/恢复服务、SQLite WAL 仓库和持久化失败可见告警；数据库连接不跨线程。
- 建立配置驱动的正式 PyQt 单站窗口，包含总览/进路、轨道编码、信号、应答器与 LEU、临时限速、区间改方、网络、日志告警八页。
- 明确分开展示逻辑报文、教学位流与 `simulation_envelope`，不冒充现场 1023 位报文。
- 完成主线程运行时桥接，接入收发计数、对站同步和阶段 5 改方协议；修复增量边界消息误要求方向字段的问题。
- 动态修复 macOS 中文路径下 Qt 插件路径，不硬编码用户名或安装目录。
- 增加窗口关闭时先停止网络线程、再关闭持久化资源的生命周期测试。
- 独立复审发现的启动锁闭、同版本同步、权威持久化原子性、恢复证据、
  REJECT/超时、坏包顺序、关闭失败和队列满等问题均已增加回归并修复。
- A 站权威方向在内存 APPLY/COMMIT 前落盘；读取异常阻止启动，重启后恢复
  方向并重发持久化恢复证据，禁止用默认方向覆盖未知历史真值。
- 当前全量回归 `183 passed`（含本机回环双站网络测试）；A/B 配置校验、
  新代码编译和 `git diff --check` 均通过。

阶段决定：应用启动、通信异常、对站快照过期、改方事务未确认和方向恢复
未完成时均保持安全锁闭。A 站权威方向必须先持久化再 APPLY/COMMIT；读取失败
直接阻止启动，不允许默认方向覆盖未知历史真值。改方期间合并延迟状态同步，
事务结束后只补发一次最终快照；恢复证据在收到更强的应用确认机制前长期保留，
允许重连幂等重发。

## 已完成：阶段 7

已完成内容：

- 新增配置驱动列车演示服务和正式 UI 页，轨道占用只经控制器 `TRAIN` 来源写入。
- 建立七类答辩场景目录及详细操作、预期、复位文档。
- 建立先 A 后 B 的双站启动器；A 在 `bind + listen` 后发布本次进程的一次性
  就绪凭据，避免把其他程序占用端口误判为 A 就绪；跨平台隔离进程组并实测干净回收。
- 完成从零安装、Qt 诊断、单/双站启动、测试和免责声明 README。
- 正式包无顶层旧包导入后，将初始 `models/services/network/ui` 用 Git 完整归档到 `legacy/`。
- 生成并目视检查 A/B 正式窗口合成状态截图，并明确它只证明 UI 外观，真实
  握手/同步由双进程演练和集成测试证明；验收记录见 `docs/acceptance/STAGE7_ACCEPTANCE.md`。
- 当前全量回归 `196 passed`，实际双站离屏启动/握手/关闭演练无异常。
- 最终独立复审确认就绪凭据、配置传递、跨平台进程隔离和证据边界修复有效，
  未发现 Critical/Important；功能提交 `1169ac4` 已推送。

原七个 CTCS-2 功能阶段均已完成；当前按 `docs/dual-station-plans/` 执行新的
双站同屏六阶段改造。必须从上表首个“进行中/未开始”阶段继续，不重复已完成阶段。

## 恢复命令

```bash
cd /Users/zhu/Desktop/TCC-project/.worktrees/ctcs2-rebuild
git status --short
git log --oneline --decorate -10
sed -n '1,240p' docs/IMPLEMENTATION_STATUS.md
```
